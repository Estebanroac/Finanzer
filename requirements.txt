fastapi==0.115.0
uvicorn==0.30.0
pydantic>=2.0
curl_cffi>=0.7.0
reportlab>=4.0
yfinance>=0.2.36
pandas>=2.0
# Directly imported (numpy in financial_ratios, requests in data_fetcher);
# were only available transitively via pandas/yfinance — pin explicitly.
numpy>=1.24
requests>=2.31
